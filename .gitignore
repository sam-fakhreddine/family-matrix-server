# Deployment identity & secrets — NEVER commit
.env

# Rendered per-deployment configs (generated by setup.sh from templates/)
files/
coturn/turnserver.conf
livekit/livekit.yaml
synapse-admin-config.json
ingress/traefik-rendered.yml

# Data directories
schemas/
*.dump
*.tar.gz

# Runtime litter
.backup.lock
backup.log
ingress/letsencrypt/

# Anything credential-shaped
*credentials*
*.key
acme.json
